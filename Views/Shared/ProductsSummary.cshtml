@model Product

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - BeerShop</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/Content/product.css" />
</head>

@*<div class="container">
    <div class="row">
        <div class="col-3">*@
            <div class="thumbnail">
                <img src="@Model.ImageUrl" style="width:90%;" alt="" />
                <div class="caption">
                    <h5 style="text-align:center; margin-top:1%; color:black;"> <a>@Model.Name</a></h5>
                    <h3 class="pull-right">@Model.Price.ToString("c")</h3>
                </div>
            </div>
            <div class="addToCart text-right">
                <p class="button">
                    <a class="btn btn-success" id="cartButton"
                       asp-controller="ShoppingCart"
                       asp-action="AddToShoppingCart"
                       asp-route-productId="@Model.ProductId">
                        Add to cart
                    </a>
                </p>
            </div>
        @*</div>
    </div>
</div>*@
@*<div class="container-fluid" style="margin-top:10%">
        <div class="row">
            <div class="col-3" style="width:400px; padding:0;">
                <a asp-action="Index" asp-controller="Product">
                    <div>
                        <img src="@Model.ImageUrl" style="width:90%; filter: blur(2px); -webkit-filter: blur(2px);" />
                        <h5 style="text-align:center; margin-top:1%; color:black;">@Model.Name</h5>
                    </div>
                </a>
                <h5>Description</h5>
                <div class="row">
                    <div class="col-6">
                        <h6>Romania</h6>
                        <h6>Blonda</h6>
                        <h6>6%</h6>
                    </div>
                    <div class="col-6">
                        <h6>Blonde Ale</h6>
                        <h6>33cl</h6>
                        <h6>@Model.Price.ToString("c")</h6>
                    </div>
                </div>
                <div class="vertical-center">
                    <p class="button">
                        <a class="btn btn-success" id="cartButton"
                           asp-controller="ShoppingCart"
                           asp-action="AddToShoppingCart"
                           asp-route-productId="@Model.ProductId">
                            Add to cart
                        </a>
                    </p>
                </div>
            </div>


         </div>
    </div>*@
